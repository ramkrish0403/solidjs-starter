import Button from '@suid/material/Button';

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <Button variant="contained" color="success">
        Success
      </Button>
      {/* Content for Profile Sub Page 1 */}
    </div>
  );
};

export default Dashboard;